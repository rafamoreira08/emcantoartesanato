name: Build and Deploy to Production

on:
  push:
    branches:
      - claude-improvements

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: claude-improvements
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          JS_FILE=$(ls dist/assets/index-*.js | xargs basename)
          CSS_FILE=$(ls dist/assets/index-*.css | xargs basename)

          git fetch origin main
          git checkout main

          # Copy new assets
          cp dist/assets/$JS_FILE assets/
          cp dist/assets/$CSS_FILE assets/

          # Copy production index.html (already has correct relative paths from vite base: './')
          cp dist/index.html index.html

          # Sync images folder (from public/ via dist/)
          mkdir -p images
          cp -r dist/images/. images/

          # Use -f (force) because assets/ and images/ are in .gitignore
          # but we need to commit them for production deployment
          git add -f assets/$JS_FILE assets/$CSS_FILE index.html images/
          git diff --staged --quiet || git commit -m "Deploy: automated build from $(git rev-parse --short ORIG_HEAD)"
          git push origin main
